<?php

namespace giantbits\crelish\plugins\jsoneditornew;

use giantbits\crelish\plugins\jsoneditor\JsonEditorContentProcessor;

/**
 * JSON Editor New content processor
 * Extends the base JsonEditorContentProcessor since the data handling is identical
 * Only the UI widget is different
 */
class JsonEditorNewContentProcessor extends JsonEditorContentProcessor
{
  // All functionality inherited from JsonEditorContentProcessor
  // The data format and processing is identical, only the UI widget differs
}